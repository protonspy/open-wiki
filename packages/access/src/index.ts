// The project access module — the one implementation imported by the
// application, the CLI and the MCP process (plan task 9.1). Public API is
// re-exported here as it is built, group by group.
export {};
