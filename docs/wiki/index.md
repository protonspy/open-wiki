# Index

What this project knows that is not in the code and not in a spec. Every page is reachable
from here; a page that is not is a page nobody will find again.

## The harnesses

- [[what-a-harness-loads]] — what Claude Code, Codex and opencode each load and from
  where, read from their own source: the entry file, the convention, the MCP configuration,
  and what can refuse a write before it lands.

## Distribution and integration

- [[claude-code-plugins]] — how a Claude Code plugin is built, installed and distributed;
  the two things it cannot carry, which is why the product cannot ship its own write gate
  whole; and the hook mechanics that gate rests on.
