// Skeleton. The WASAPI recorder is plan group 4
// (`adr:0005-wasapi-capture-in-a-minimal-sidecar`). Until then this binary
// refuses to run rather than pretending to record.
fn main() {
    eprintln!("recorder: not implemented (plan group 4)");
    std::process::exit(1);
}